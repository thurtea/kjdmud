// made by the wand of creation
inherit "/inherit/item";
void create() {
    set_short("a slag bin");
    set_long("A dented bin chained to a post. It does not budge.\n");
    set_ids(({ "slag_bin", "bin" }));
    set_prevent_get(1);
    set_weight(1);
    set_value(1);
}

# Next Session Handoff: 2026-09-17

Session scratch lives in gitignored `notes/tomorrow.md` (CURSOR.md
rule 5). That file has the full progress, next steps, and a
copy-paste pickup prompt.

Short status: TMI-2 boots Ready; live login creates a character;
`say` and `inventory` work; `look` fails because `I3`/`channels`
create fails and `user::setup` never places the player in a room.
Next: fix that path, then re-probe look/move.

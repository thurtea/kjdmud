// made by the wand of creation
inherit "/inherit/item";
void create() {
    set_short("a ration tin");
    set_long("A stamped tin of soy paste. Still sealed.\n");
    set_ids(({ "ration_tin", "tin", "ration" }));
    set_weight(1);
    set_value(1);
}
